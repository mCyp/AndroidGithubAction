name: Android CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: set up JDK 1.8
      uses: actions/setup-java@v2
      with:
        java-version: 8.0.232+9.1
        distribution: 'adopt'
        cache: gradle

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
      
    - name: Build with Gradle
      run: ./gradlew build
      
    - name: Send mailer
      uses: devellany/send-mail@v1.0.2
      if: ${{ success() }}
      with: 
        host: smtp.qq.com
        port: 465
        account: 200522649@@qq.com
        password: lmammqyeiunrbjae
        sender: 200522649@qq.com
        from: 200522649@qq.com
        to: 2556536414@qq.com
        subject: 这是一封秘密邮件
        body: 打包成功！
